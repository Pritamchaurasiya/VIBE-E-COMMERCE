
import React from 'react';
import { Box, Typography } from '@mui/material';

export const SupplyChainTracker = () => (
  <Box p={2}>
    <Typography variant="h6">Supply Chain Tracker</Typography>
    <Typography>Real-time shipment tracking coming soon.</Typography>
  </Box>
);

export const SecurityDashboard = () => (
  <Box p={2}>
    <Typography variant="h6">Security Dashboard</Typography>
    <Typography>Security monitoring enabled.</Typography>
  </Box>
);

export const AnalyticsDashboard = () => (
  <Box p={2}>
    <Typography variant="h6">Analytics Dashboard</Typography>
    <Typography>Analytics data visualization.</Typography>
  </Box>
);
