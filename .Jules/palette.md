## 2024-05-23 - Accessible Toggle Buttons
**Learning:** Custom toggle buttons (like packing options in `AgriProductCard`) need explicit `aria-pressed` state and descriptive labels to communicate selection to screen readers.
**Action:** Always add `aria-pressed` and dynamic `aria-label` to custom selection buttons that act like radio groups or toggles.
