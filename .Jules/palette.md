## 2024-05-22 - Empty State for Product Lists
**Learning:** Users often land on empty product lists when filtering too aggressively. A generic "0 items" text is discouraging and lacks a recovery path.
**Action:** Implemented a reusable `EmptyState` component with a clear call to action ("Clear Filters") to keep the user engaged and reduce bounce rates on empty search results.
