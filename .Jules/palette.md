## 2024-05-22 - Dynamic State Labels
**Learning:** Toggle buttons for product variants (like packing size) often rely on visual cues (color change) for state. Using `aria-pressed` and dynamic `aria-label` makes this state accessible to screen readers without changing the visual design.
**Action:** Always check variant selectors for accessible state indicators.
